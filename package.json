{
  "name": "poker-vision-console",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 4173",
    "build": "tsc --noEmit && vite build",
    "build:pages": "npm run build -- --mode pages",
    "check:pages": "node scripts/check-pages.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:recording": "node scripts/test-recording.mjs",
    "test:python": "uv run pytest -q",
    "check:architecture": "node scripts/check-architecture.mjs",
    "verify": "npm run typecheck && npm run lint && npm run check:architecture && npm test && npm run test:python && npm run build && npm run smoke:production",
    "lint": "prettier --check web scripts/*.mjs scripts/*.ts vite.config.ts tsconfig.json package.json && uv run ruff check app/vision tests/vision scripts/smoke-production.py",
    "format": "prettier --write web scripts/*.mjs scripts/*.ts vite.config.ts tsconfig.json package.json && uv run ruff format app/vision tests/vision scripts/smoke-production.py",
    "benchmark": "vite-node scripts/benchmark.ts",
    "verify:step": "node scripts/verify-step.mjs",
    "smoke:production": "uv run python scripts/smoke-production.py",
    "predev": "node scripts/prepare-ocr.mjs",
    "prebuild": "node scripts/prepare-ocr.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.18.6",
    "@types/pngjs": "6.0.5",
    "pngjs": "7.0.0",
    "prettier": "3.6.2",
    "typescript": "~5.9.3",
    "vite": "^7.1.7",
    "vitest": "^3.2.4"
  },
  "dependencies": {
    "@tesseract.js-data/eng": "1.0.0",
    "tesseract.js": "7.0.0"
  }
}
